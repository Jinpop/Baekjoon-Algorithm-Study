#include <iostream>
#include <queue>
#include <vector>
#include <algorithm>
using namespace std;

int main()
{
    cin.tie(0);
    cout.tie(0);
    ios_base::sync_with_stdio(0);

    cout << fixed;
    cout.precision(1);

    double a, b;
    cin >> a >> b;
    
    cout << a*b/2;

    return 0;
}